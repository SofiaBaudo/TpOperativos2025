#include <variables_globales_cpu.h>

void creacionNivelesN(int n);
//hay que ver si va la funicon traduccion, hablar en el soporte. SOmos nosotros los que hacen la traduccion o memoria
uint32_t traduccion(int desplazamiento, nivel niveles[], int cantNiveles, uint32_t dirLogica);